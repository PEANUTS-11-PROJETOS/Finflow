# Carteira — Pauta do dia — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Criar a aba **Carteira** — pauta diária navegável com baixa rápida de pagamento (tudo/parcial/só juros) e atalho pro WhatsApp — e torná-la a aba principal do app.

**Architecture:** Server Component em `/carteira?dia=YYYY-MM-DD` consulta as parcelas do dia e renderiza linhas (Client Components) que chamam as server actions de pagamento **já existentes** (`pagarTudo`, `pagarJuros`, `pagarParcial`, `marcarParcela`). Paginação por dia via navegação com `prefetch`. Segue o padrão da atual `/a-receber-hoje`.

**Tech Stack:** Next.js 16 (App Router, Server Components, Server Actions), React 19, Supabase, Tailwind v4, Base UI, lucide-react, sonner.

**Verificação (importante):** este projeto **não tem test runner** (scripts: `dev`, `build`, `lint`). Seguindo o padrão do código, cada task é verificada com:
- `npx tsc --noEmit` (tipos)
- `npx eslint <arquivos alterados>` (lint)
- ao final, `npm run build` + verificação manual no dev server

Não adicione framework de testes — foge do padrão do projeto.

**Referências de padrão (leia antes de começar):**
- `src/components/dashboard/card-cobranca-renovavel.tsx` — padrão exato de chamar as actions (useTransition + toast + `router.refresh()`), input de parcial e validação.
- `src/app/(dashboard)/a-receber-hoje/page.tsx` — padrão de página de lista (server component).
- `src/app/(dashboard)/emprestimos/actions.ts` — as 4 actions reaproveitadas.
- Spec: `docs/superpowers/specs/2026-07-08-carteira-design.md`.

---

## Task 1: Migração — coluna `baixa` em `parcelas`

**Files:**
- Migration: Supabase SQL Editor (manual — padrão do projeto)
- Modify: `src/types/index.ts`

- [ ] **Step 1: Rodar a migration no Supabase**

No Supabase → SQL Editor, rode:

```sql
alter table parcelas
  add column if not exists baixa text
  check (baixa in ('tudo','parcial','juros'));
```

Confirme "Success". (A constraint aceita `null` normalmente — parcela em aberto tem `baixa = null`.)

- [ ] **Step 2: Adicionar o campo no tipo `Parcela`**

Em `src/types/index.ts`, na interface `Parcela`, adicione o campo `baixa` logo após `data_pagamento`:

```ts
export interface Parcela {
  id: string
  emprestimo_id: string
  credor_id: string
  numero: number
  valor: number
  valor_juros: number | null
  vencimento: string
  pago: boolean
  rolado: boolean
  data_pagamento: string | null
  baixa: 'tudo' | 'parcial' | 'juros' | null
  created_at: string
}
```

- [ ] **Step 3: Verificar tipos**

Run: `npx tsc --noEmit`
Expected: exit 0, sem erros.

- [ ] **Step 4: Commit**

```bash
git add src/types/index.ts
git commit -m "feat: coluna baixa em parcelas (tudo/parcial/juros)"
```

---

## Task 2: As 4 actions gravam `baixa` e revalidam `/carteira`

**Files:**
- Modify: `src/app/(dashboard)/emprestimos/actions.ts`

- [ ] **Step 1: `pagarParcial` — gravar `baixa: 'parcial'` nos dois updates de pago**

Em `pagarParcial`, no bloco `if (novoSaldo <= 0)` (quitação), altere o update da parcela:

```ts
    await supabase.from('parcelas')
      .update({ pago: true, data_pagamento: new Date().toISOString().split('T')[0], baixa: 'parcial' })
      .eq('id', parcelaId)
```

E logo abaixo, no update que marca a parcela paga no fluxo normal:

```ts
  const { error: e1 } = await supabase.from('parcelas')
    .update({ pago: true, data_pagamento: new Date().toISOString().split('T')[0], baixa: 'parcial' })
    .eq('id', parcelaId)
```

- [ ] **Step 2: `pagarJuros` — gravar `baixa: 'juros'`**

Em `pagarJuros`, altere o update que marca `rolado`:

```ts
  const { error: e1 } = await supabase.from('parcelas')
    .update({ rolado: true, data_pagamento: new Date().toISOString().split('T')[0], baixa: 'juros' })
    .eq('id', parcelaId)
```

- [ ] **Step 3: `pagarTudo` — gravar `baixa: 'tudo'`**

Em `pagarTudo`, altere o update da parcela:

```ts
  await supabase.from('parcelas')
    .update({ pago: true, data_pagamento: new Date().toISOString().split('T')[0], baixa: 'tudo' })
    .eq('id', parcelaId)
```

- [ ] **Step 4: `marcarParcela` — gravar `baixa` conforme pago**

Em `marcarParcela`, altere o update:

```ts
  const { error } = await supabase.from('parcelas')
    .update({ pago, data_pagamento: pago ? new Date().toISOString().split('T')[0] : null, baixa: pago ? 'tudo' : null })
    .eq('id', parcelaId).eq('credor_id', user.id)
```

- [ ] **Step 5: Revalidar `/carteira` nas 4 actions**

Em cada uma das 4 actions (`pagarParcial`, `pagarJuros`, `pagarTudo`, `marcarParcela`), adicione uma linha `revalidatePath('/carteira')` junto dos `revalidatePath` existentes (antes do `return { success: ... }`). Exemplo em `marcarParcela`:

```ts
  revalidatePath('/emprestimos')
  revalidatePath('/carteira')
  return { success: true }
```

Faça o mesmo nas outras três (adicionar `revalidatePath('/carteira')` ao lado dos existentes).

- [ ] **Step 6: Verificar tipos e lint**

Run: `npx tsc --noEmit && npx eslint "src/app/(dashboard)/emprestimos/actions.ts"`
Expected: exit 0.

- [ ] **Step 7: Commit**

```bash
git add "src/app/(dashboard)/emprestimos/actions.ts"
git commit -m "feat: actions de pagamento gravam baixa e revalidam /carteira"
```

---

## Task 3: Helper de link do WhatsApp de cobrança

**Files:**
- Create: `src/lib/whatsapp-link.ts`

- [ ] **Step 1: Criar o helper**

`src/lib/whatsapp-link.ts`:

```ts
import { fmtData } from '@/lib/utils'

/**
 * Monta o link wa.me com uma mensagem de cobrança pronta.
 * Retorna null quando o cliente não tem telefone.
 */
export function linkCobranca(
  telefone: string | null,
  nomeCliente: string,
  valorParcela: number,
  vencimento: string,
): string | null {
  if (!telefone) return null
  const valor = new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' }).format(valorParcela)
  const venc = fmtData(vencimento)
  const msg =
    `Olá ${nomeCliente}! Passando para lembrar da parcela do seu empréstimo 📋\n\n` +
    `Valor: ${valor}\nVencimento: ${venc}\n\nQualquer dúvida estou à disposição! 😊`
  return `https://wa.me/55${telefone.replace(/\D/g, '')}?text=${encodeURIComponent(msg)}`
}
```

- [ ] **Step 2: Verificar tipos e lint**

Run: `npx tsc --noEmit && npx eslint src/lib/whatsapp-link.ts`
Expected: exit 0.

- [ ] **Step 3: Commit**

```bash
git add src/lib/whatsapp-link.ts
git commit -m "feat: helper linkCobranca para WhatsApp"
```

---

## Task 4: Modal de pagamento parcial

**Files:**
- Create: `src/components/carteira/parcial-modal.tsx`

- [ ] **Step 1: Criar o modal**

`src/components/carteira/parcial-modal.tsx`:

```tsx
'use client'
import { useState, useTransition } from 'react'
import { toast } from 'sonner'
import { Dialog, DialogContent, DialogHeader, DialogTitle } from '@/components/ui/dialog'
import { Button } from '@/components/ui/button'
import { Money } from '@/components/ui/money'
import { pagarParcial } from '@/app/(dashboard)/emprestimos/actions'
import { fmtMoeda } from '@/lib/utils'

const INPUT_CLS =
  'h-10 w-full rounded-lg border border-input bg-background px-3 text-sm tabular-nums outline-none transition-colors focus-visible:border-ring focus-visible:ring-4 focus-visible:ring-ring/10'

interface Props {
  parcelaId: string
  valorJuros: number
  valorPrincipal: number
  open: boolean
  onOpenChange: (v: boolean) => void
  onDone: () => void
}

export function ParcialModal({ parcelaId, valorJuros, valorPrincipal, open, onOpenChange, onDone }: Props) {
  const [pending, startTransition] = useTransition()
  const [valorPago, setValorPago] = useState('')

  const vp = parseFloat(valorPago) || 0
  const abatimento = Math.max(0, vp - valorJuros)
  const novoSaldo = Math.max(0, valorPrincipal - abatimento)
  const valido = vp >= valorJuros && vp > 0

  function confirmar() {
    startTransition(async () => {
      const r = await pagarParcial(parcelaId, vp)
      if (r?.error) { toast.error(String(r.error)); return }
      if (r.quitado) toast.success('Empréstimo quitado com pagamento parcial!')
      else toast.success(`Pagamento registrado. Novo saldo: ${fmtMoeda(r.novoSaldo ?? 0)}`)
      setValorPago('')
      onOpenChange(false)
      onDone()
    })
  }

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent>
        <DialogHeader>
          <DialogTitle>Quanto o cliente pagou?</DialogTitle>
        </DialogHeader>
        <input
          type="number"
          step="0.01"
          min={valorJuros}
          placeholder={`Mín. ${fmtMoeda(valorJuros)} (juros)`}
          value={valorPago}
          onChange={e => setValorPago(e.target.value)}
          className={INPUT_CLS}
          autoFocus
        />
        {vp > 0 && (
          <div className="space-y-1.5 text-xs">
            <div className="flex justify-between">
              <span className="text-muted-foreground">Abate do principal</span>
              <Money value={abatimento} tone="success" />
            </div>
            <div className="flex justify-between font-medium border-t pt-1.5">
              <span>Novo saldo devedor</span>
              <Money value={novoSaldo} />
            </div>
            {!valido && (
              <p className="text-destructive">Valor mínimo: {fmtMoeda(valorJuros)} (cobre os juros)</p>
            )}
          </div>
        )}
        <div className="flex gap-2">
          <Button disabled={pending || !valido} onClick={confirmar} className="flex-1">
            {pending ? 'Registrando...' : 'Confirmar'}
          </Button>
          <Button variant="outline" disabled={pending} onClick={() => onOpenChange(false)}>
            Cancelar
          </Button>
        </div>
      </DialogContent>
    </Dialog>
  )
}
```

- [ ] **Step 2: Verificar tipos e lint**

Run: `npx tsc --noEmit && npx eslint src/components/carteira/parcial-modal.tsx`
Expected: exit 0.

- [ ] **Step 3: Commit**

```bash
git add src/components/carteira/parcial-modal.tsx
git commit -m "feat: modal de pagamento parcial da Carteira"
```

---

## Task 5: Linha da pauta (item + ações)

**Files:**
- Create: `src/components/carteira/pauta-row.tsx`

Define o tipo `PautaItem` (exportado, usado pela página) e o componente da linha, responsivo (funciona como linha no desktop e card no mobile). Ações renováveis: Tudo/Parcial/Só juros; Price: só Tudo. "Tudo" e "Só juros" têm mini-confirmação de 2 toques.

- [ ] **Step 1: Criar a linha**

`src/components/carteira/pauta-row.tsx`:

```tsx
'use client'
import { useState, useTransition } from 'react'
import { useRouter } from 'next/navigation'
import { toast } from 'sonner'
import { CheckCircle2, RefreshCw, SplitSquareHorizontal, MessageCircle } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { Money } from '@/components/ui/money'
import { fmtMoeda, cn } from '@/lib/utils'
import { linkCobranca } from '@/lib/whatsapp-link'
import { pagarTudo, pagarJuros, marcarParcela } from '@/app/(dashboard)/emprestimos/actions'
import { ParcialModal } from './parcial-modal'

export interface PautaItem {
  parcelaId: string
  tipo: 'price' | 'renovavel'
  clienteNome: string
  clienteTelefone: string | null
  valorPego: number      // principal do empréstimo
  valorParcela: number   // total do vencimento
  valorJuros: number     // juros da parcela (0 no price)
  vencimento: string
  baixa: 'tudo' | 'parcial' | 'juros' | null
  pago: boolean
  rolado: boolean
}

function iniciais(nome: string) {
  return nome.split(' ').slice(0, 2).map(n => n[0]).join('').toUpperCase()
}

export function PautaRow({ item }: { item: PautaItem }) {
  const router = useRouter()
  const [pending, startTransition] = useTransition()
  const [confirmar, setConfirmar] = useState<'tudo' | 'juros' | null>(null)
  const [parcialOpen, setParcialOpen] = useState(false)

  const baixado = item.pago || item.rolado
  const renovavel = item.tipo === 'renovavel'
  const wa = linkCobranca(item.clienteTelefone, item.clienteNome, item.valorParcela, item.vencimento)

  function aplicar(acao: 'tudo' | 'juros') {
    startTransition(async () => {
      const r = acao === 'tudo'
        ? (renovavel ? await pagarTudo(item.parcelaId) : await marcarParcela(item.parcelaId, true))
        : await pagarJuros(item.parcelaId)
      if (r?.error) { toast.error('Erro ao registrar pagamento'); return }
      toast.success(acao === 'tudo' ? 'Pagamento registrado!' : 'Juros registrados. Principal rolou.')
      setConfirmar(null)
      router.refresh()
    })
  }

  const rotuloBaixa = item.rolado
    ? 'Rolou só os juros'
    : item.baixa === 'parcial'
      ? 'Pagou parcial'
      : 'Pagou tudo'

  return (
    <div className={cn(
      'flex flex-col gap-3 rounded-xl border bg-card px-4 py-3 sm:flex-row sm:items-center sm:gap-4',
      baixado && 'opacity-60',
    )}>
      {/* Cliente + valores */}
      <div className="flex items-center gap-3 sm:flex-1 sm:min-w-0">
        <div className="h-10 w-10 flex-none rounded-full bg-muted text-foreground/80 flex items-center justify-center text-sm font-semibold">
          {iniciais(item.clienteNome)}
        </div>
        <div className="min-w-0 flex-1">
          <p className="font-medium truncate">{item.clienteNome}</p>
          <p className="text-xs text-muted-foreground mt-0.5">
            Pegou <Money value={item.valorPego} /> · Parcela <Money value={item.valorParcela} />
          </p>
        </div>
      </div>

      {/* Ações / estado */}
      {baixado ? (
        <div className="flex items-center gap-2 sm:justify-end">
          <span className="inline-flex items-center gap-1.5 text-xs font-medium text-[var(--success)]">
            <CheckCircle2 className="h-4 w-4" /> {rotuloBaixa}
          </span>
          {wa && (
            <a href={wa} target="_blank" rel="noopener noreferrer">
              <Button variant="outline" size="icon-sm" className="text-[var(--success)]">
                <MessageCircle className="h-4 w-4" />
              </Button>
            </a>
          )}
        </div>
      ) : confirmar ? (
        <div className="flex items-center gap-2 sm:justify-end">
          <span className="text-xs text-muted-foreground">
            Confirmar {confirmar === 'tudo' ? fmtMoeda(item.valorParcela) : `juros de ${fmtMoeda(item.valorJuros)}`}?
          </span>
          <Button size="sm" disabled={pending} onClick={() => aplicar(confirmar)}>
            {pending ? '...' : 'Confirmar'}
          </Button>
          <Button size="sm" variant="outline" disabled={pending} onClick={() => setConfirmar(null)}>
            Cancelar
          </Button>
        </div>
      ) : (
        <div className="flex flex-wrap items-center gap-2 sm:justify-end">
          <Button size="sm" disabled={pending} onClick={() => setConfirmar('tudo')} className="gap-1.5">
            <CheckCircle2 className="h-4 w-4" /> Tudo
          </Button>
          {renovavel && (
            <>
              <Button size="sm" variant="outline" disabled={pending} onClick={() => setParcialOpen(true)} className="gap-1.5">
                <SplitSquareHorizontal className="h-4 w-4" /> Parcial
              </Button>
              <Button size="sm" variant="outline" disabled={pending} onClick={() => setConfirmar('juros')} className="gap-1.5">
                <RefreshCw className="h-4 w-4" /> Só juros
              </Button>
            </>
          )}
          <a href={wa ?? undefined} target={wa ? '_blank' : undefined} rel="noopener noreferrer" aria-disabled={!wa}>
            <Button variant="outline" size="icon-sm" disabled={!wa} className="text-[var(--success)]">
              <MessageCircle className="h-4 w-4" />
            </Button>
          </a>
        </div>
      )}

      {renovavel && (
        <ParcialModal
          parcelaId={item.parcelaId}
          valorJuros={item.valorJuros}
          valorPrincipal={item.valorPego}
          open={parcialOpen}
          onOpenChange={setParcialOpen}
          onDone={() => router.refresh()}
        />
      )}
    </div>
  )
}
```

- [ ] **Step 2: Verificar tipos e lint**

Run: `npx tsc --noEmit && npx eslint src/components/carteira/pauta-row.tsx`
Expected: exit 0. (Se o lint reclamar de `cn` não exportado de `@/lib/utils`, confirme o import — `cn` e `fmtMoeda` são exportados de lá, como em card-cobranca-renovavel.tsx.)

- [ ] **Step 3: Commit**

```bash
git add src/components/carteira/pauta-row.tsx
git commit -m "feat: PautaRow com acoes de baixa rapida e WhatsApp"
```

---

## Task 6: Navegação por dia (‹ › + trocar dia)

**Files:**
- Create: `src/components/carteira/day-nav.tsx`

- [ ] **Step 1: Criar o componente**

`src/components/carteira/day-nav.tsx`:

```tsx
'use client'
import Link from 'next/link'
import { useRouter } from 'next/navigation'
import { ChevronLeft, ChevronRight, CalendarDays } from 'lucide-react'
import { Button } from '@/components/ui/button'

function shift(dia: string, delta: number): string {
  const d = new Date(dia + 'T12:00:00')
  d.setDate(d.getDate() + delta)
  return d.toISOString().split('T')[0]
}

export function DayNav({ dia }: { dia: string }) {
  const router = useRouter()
  const d = new Date(dia + 'T12:00:00')
  const dataBR = d.toLocaleDateString('pt-BR')
  const semana = d.toLocaleDateString('pt-BR', { weekday: 'long' })

  return (
    <div className="flex items-center gap-3">
      <Button variant="outline" size="icon" render={<Link href={`/carteira?dia=${shift(dia, -1)}`} prefetch />}>
        <ChevronLeft className="h-4 w-4" />
      </Button>

      <div className="min-w-0">
        <p className="eyebrow">Pauta do dia</p>
        <p className="text-2xl font-serif-display leading-none">{dataBR}</p>
        <p className="text-xs text-muted-foreground first-letter:uppercase">{semana}</p>
      </div>

      <Button variant="outline" size="icon" render={<Link href={`/carteira?dia=${shift(dia, 1)}`} prefetch />}>
        <ChevronRight className="h-4 w-4" />
      </Button>

      <label className="ml-auto inline-flex items-center gap-1.5 rounded-lg border px-3 h-8 text-sm cursor-pointer hover:bg-muted transition-colors">
        <CalendarDays className="h-4 w-4" />
        Trocar dia
        <input
          type="date"
          value={dia}
          onChange={e => { if (e.target.value) router.push(`/carteira?dia=${e.target.value}`) }}
          className="sr-only"
        />
      </label>
    </div>
  )
}
```

> Nota: `Button render={<Link ... />}` é o padrão polimórfico do Base UI já usado em `emprestimos/[id]/page.tsx`.

- [ ] **Step 2: Verificar tipos e lint**

Run: `npx tsc --noEmit && npx eslint src/components/carteira/day-nav.tsx`
Expected: exit 0.

- [ ] **Step 3: Commit**

```bash
git add src/components/carteira/day-nav.tsx
git commit -m "feat: DayNav (paginacao por dia da Carteira)"
```

---

## Task 7: Página `/carteira` + loading

**Files:**
- Create: `src/app/(dashboard)/carteira/page.tsx`
- Create: `src/app/(dashboard)/carteira/loading.tsx`

- [ ] **Step 1: Criar a página (server component)**

`src/app/(dashboard)/carteira/page.tsx`:

```tsx
import { createClient } from '@/lib/supabase/server'
import { redirect } from 'next/navigation'
import { Card, CardContent } from '@/components/ui/card'
import { Money } from '@/components/ui/money'
import { DayNav } from '@/components/carteira/day-nav'
import { PautaRow, type PautaItem } from '@/components/carteira/pauta-row'

type EmpJoin = {
  id: string
  tipo: 'price' | 'renovavel'
  valor_principal: number
  clientes: { nome: string; telefone: string | null } | null
}
type ParcelaJoin = {
  id: string
  valor: number
  valor_juros: number | null
  vencimento: string
  pago: boolean
  rolado: boolean
  baixa: 'tudo' | 'parcial' | 'juros' | null
  emprestimos: EmpJoin | null
}

export default async function CarteiraPage({
  searchParams,
}: {
  searchParams: Promise<{ dia?: string }>
}) {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')

  const { dia: diaParam } = await searchParams
  const dia = diaParam ?? new Date().toISOString().split('T')[0]

  const { data } = await supabase
    .from('parcelas')
    .select('id, valor, valor_juros, vencimento, pago, rolado, baixa, emprestimos(id, tipo, valor_principal, clientes(nome, telefone))')
    .eq('credor_id', user.id)
    .eq('vencimento', dia)

  const itens: PautaItem[] = []
  for (const p of (data ?? []) as unknown as ParcelaJoin[]) {
    const emp = p.emprestimos
    if (!emp?.clientes) continue
    itens.push({
      parcelaId: p.id,
      tipo: emp.tipo,
      clienteNome: emp.clientes.nome,
      clienteTelefone: emp.clientes.telefone,
      valorPego: Number(emp.valor_principal),
      valorParcela: Number(p.valor),
      valorJuros: Number(p.valor_juros ?? 0),
      vencimento: p.vencimento,
      baixa: p.baixa,
      pago: p.pago,
      rolado: p.rolado,
    })
  }

  // Em aberto primeiro, baixados embaixo
  itens.sort((a, b) => Number(a.pago || a.rolado) - Number(b.pago || b.rolado))

  const aReceber = itens.reduce((s, i) => s + i.valorParcela, 0)
  const baixados = itens.filter(i => i.pago || i.rolado).length

  return (
    <div className="space-y-6">
      <div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between">
        <DayNav dia={dia} />
        <div className="flex gap-3">
          <Card className="min-w-[9rem]">
            <CardContent className="py-3 px-4">
              <p className="eyebrow">A receber</p>
              <p className="text-xl font-mono mt-1"><Money value={aReceber} /></p>
            </CardContent>
          </Card>
          <Card className="min-w-[6rem]">
            <CardContent className="py-3 px-4">
              <p className="eyebrow">Baixados</p>
              <p className="text-xl font-mono mt-1">{baixados}/{itens.length}</p>
            </CardContent>
          </Card>
        </div>
      </div>

      {itens.length === 0 ? (
        <Card>
          <CardContent className="py-16 text-center space-y-2">
            <p className="text-4xl">☕</p>
            <p className="text-sm font-medium">Nenhuma cobrança nesse dia</p>
          </CardContent>
        </Card>
      ) : (
        <div className="space-y-2">
          {itens.map(item => (
            <PautaRow key={item.parcelaId} item={item} />
          ))}
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 2: Criar o loading (skeleton)**

`src/app/(dashboard)/carteira/loading.tsx`:

```tsx
export default function Loading() {
  return (
    <div className="space-y-6">
      <div className="h-16 w-64 rounded-lg bg-muted animate-pulse" />
      <div className="space-y-2">
        {Array.from({ length: 5 }).map((_, i) => (
          <div key={i} className="h-16 rounded-xl border bg-card animate-pulse" />
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Step 3: Verificar tipos e lint**

Run: `npx tsc --noEmit && npx eslint "src/app/(dashboard)/carteira/page.tsx" "src/app/(dashboard)/carteira/loading.tsx"`
Expected: exit 0.

- [ ] **Step 4: Commit**

```bash
git add "src/app/(dashboard)/carteira/page.tsx" "src/app/(dashboard)/carteira/loading.tsx"
git commit -m "feat: pagina /carteira (pauta do dia) + loading"
```

---

## Task 8: Adicionar "Carteira" na navegação (1º lugar)

**Files:**
- Modify: `src/components/dashboard/sidebar.tsx`

- [ ] **Step 1: Importar o ícone e adicionar o link em 1º**

Em `src/components/dashboard/sidebar.tsx`, adicione `Wallet` no import de `lucide-react`:

```ts
import { Home, Users, HandCoins, Settings, LogOut, Wallet } from 'lucide-react'
```

E coloque a Carteira como primeiro item de `navLinks`:

```ts
const navLinks = [
  { href: '/carteira',      label: 'Carteira',       icon: Wallet },
  { href: '/dashboard',     label: 'Painel',         icon: Home },
  { href: '/clientes',      label: 'Clientes',       icon: Users },
  { href: '/emprestimos',   label: 'Empréstimos',    icon: HandCoins },
  { href: '/configuracoes', label: 'Configurações',  icon: Settings },
]
```

- [ ] **Step 2: Verificar tipos e lint**

Run: `npx tsc --noEmit && npx eslint src/components/dashboard/sidebar.tsx`
Expected: exit 0.

- [ ] **Step 3: Commit**

```bash
git add src/components/dashboard/sidebar.tsx
git commit -m "feat: Carteira como primeiro item do menu"
```

---

## Task 9: Carteira como landing + aposentar `/a-receber-hoje`

**Files:**
- Modify: `src/app/page.tsx`
- Modify: `src/components/auth/login-form.tsx:35`
- Modify: `src/app/(auth)/redefinir-senha/page.tsx:27`
- Modify: `src/proxy.ts` (redirect de usuário logado)
- Modify: `src/app/api/auth/callback/route.ts:7`
- Modify: `src/app/admin/page.tsx:15`
- Modify: `src/app/(dashboard)/dashboard/page.tsx:144`
- Replace: `src/app/(dashboard)/a-receber-hoje/page.tsx`

- [ ] **Step 1: Root `/` → `/carteira`**

Em `src/app/page.tsx`, troque `redirect('/dashboard')` por `redirect('/carteira')`.

- [ ] **Step 2: Login → `/carteira`**

Em `src/components/auth/login-form.tsx`, troque `router.push('/dashboard')` por `router.push('/carteira')`.

- [ ] **Step 3: Redefinir senha → `/carteira`**

Em `src/app/(auth)/redefinir-senha/page.tsx`, troque `router.push('/dashboard')` por `router.push('/carteira')`.

- [ ] **Step 4: Proxy (usuário logado em /login,/signup) → `/carteira`**

Em `src/proxy.ts`, no bloco:

```ts
  if (user && ['/login', '/signup'].includes(pathname)) {
    return NextResponse.redirect(new URL('/carteira', request.url))
  }
```

- [ ] **Step 5: Auth callback default → `/carteira`**

Em `src/app/api/auth/callback/route.ts`, troque:

```ts
  const next = searchParams.get('next') ?? '/carteira'
```

- [ ] **Step 6: Admin (não-admin) → `/carteira`**

Em `src/app/admin/page.tsx`, troque `redirect('/dashboard')` por `redirect('/carteira')`.

- [ ] **Step 7: Card "A receber hoje" do dashboard → `/carteira`**

Em `src/app/(dashboard)/dashboard/page.tsx` linha ~144, troque `'/a-receber-hoje'` por `'/carteira'`:

```ts
          const href         = isAtrasado ? '/inadimplentes' : isHoje ? '/carteira' : null
```

- [ ] **Step 8: Aposentar a página `/a-receber-hoje` (redirect)**

Substitua **todo** o conteúdo de `src/app/(dashboard)/a-receber-hoje/page.tsx` por:

```tsx
import { redirect } from 'next/navigation'

export default function AReceberHojePage() {
  redirect('/carteira')
}
```

- [ ] **Step 9: Verificar tipos e lint**

Run: `npx tsc --noEmit && npx eslint src/app/page.tsx src/components/auth/login-form.tsx "src/app/(auth)/redefinir-senha/page.tsx" src/proxy.ts src/app/api/auth/callback/route.ts src/app/admin/page.tsx "src/app/(dashboard)/dashboard/page.tsx" "src/app/(dashboard)/a-receber-hoje/page.tsx"`
Expected: exit 0.

- [ ] **Step 10: Commit**

```bash
git add -A
git commit -m "feat: Carteira vira landing e /a-receber-hoje redireciona p/ /carteira"
```

---

## Task 10: Verificação final (build + manual)

**Files:** nenhum novo — verificação de ponta a ponta.

- [ ] **Step 1: Build de produção**

Run: `npm run build`
Expected: exit 0; a rota `/carteira` aparece na lista de rotas (`ƒ /carteira`) e `/a-receber-hoje` continua listada (agora só redireciona).

- [ ] **Step 2: Subir o dev server e verificar manualmente**

Run: `npm run dev` e abra `http://localhost:3000/carteira` (ou a porta indicada).

Checklist manual:
- [ ] "Carteira" aparece em 1º no menu; ao logar, cai na Carteira.
- [ ] A pauta mostra os clientes que vencem **hoje**; ‹ › trocam o dia; "Trocar dia" pula pra qualquer data.
- [ ] Numa parcela **renovável** em aberto: aparecem **Tudo / Parcial / Só juros** + WhatsApp.
- [ ] Numa parcela **price** em aberto: aparece só **Tudo** + WhatsApp.
- [ ] "Tudo" e "Só juros" pedem **confirmação** (2 toques) e, ao confirmar, a linha vira "baixada" e o contador "Baixados X/Y" sobe.
- [ ] "Parcial" abre o **modal**, valida o mínimo (juros) e registra.
- [ ] O ícone do WhatsApp abre a conversa; fica **desabilitado** quando o cliente não tem telefone.
- [ ] `/a-receber-hoje` redireciona pra `/carteira`; o card "A receber hoje" do Painel leva pra Carteira.
- [ ] Totais "A receber" e "Baixados" batem com as linhas do dia.

- [ ] **Step 3: Commit final (se houver ajuste)**

```bash
git add -A
git commit -m "chore: ajustes finais da Carteira"
```

---

## Notas de escopo

- **Sem novo bottom tab bar no mobile:** a Carteira entra na navegação existente (sidebar/drawer). O mockup mostra uma tab bar inferior, mas construí-la é uma reforma de navegação separada — fora do escopo desta v1. A Carteira já fica acessível e em 1º lugar no menu atual.
- Layout em linhas responsivas (flex) em vez de `<table>` HTML — segue o padrão da `/a-receber-hoje` e funciona igual no desktop e no mobile.
- Juros de mora diária **não** entra na pauta (existe no card de detalhe do empréstimo).
```
